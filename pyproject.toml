[project]
name = "aorta-dissection-predictor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cheartpy[all]",
    "code-pkg",
]

[tool.uv.sources]
code-pkg = { workspace = true, editable = true }
cheartpy = { workspace = true, editable = true }

[tool.uv.workspace]
members = [
    "code-pkg",
    ".deps/cheartpy"
]
